PROXY=http://your-proxy-address:port
# Nếu proxy cần authentication:
# PROXY=http://username:password@your-proxy-address:port

# Giới hạn dung lượng thư mục cache audio (MB). Vượt quá sẽ tự động xóa
# các cache cũ nhất. Mặc định 20480 (= 20 GB).
# MAX_CACHE_MB=20480
